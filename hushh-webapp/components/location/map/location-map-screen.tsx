"use client";

/** Voice-first Map screen (scaffold; composes the existing LiveMap renderer). */

export function LocationMapScreen() {
  return <p className="text-sm text-muted-foreground">Your Map is loading.</p>;
}
